version: '3.8'

services:
  backend:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - CONTACT_EMAIL=uddissh.verma@email.com
      - SMTP_HOST=smtp.gmail.com
      - SMTP_PORT=587
      - SMTP_USER=uddissh.verma@gmail.com
      - SMTP_PASS=nweo bysi yvqn casr  # ← WRAP IN QUOTES (spaces in app password)
      - FRONTEND_URL=http://localhost:5173
    volumes:
      - .:/app  # ← Move INSIDE service (was at root level)
    restart: unless-stopped
    # ← Remove duplicate 'restart' at root level
